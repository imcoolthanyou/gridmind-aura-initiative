# GridMind Aura Initiative - Environment Configuration

# Base URL for the application
# For local development with mobile access, set this to your machine's IP address
# Example: NEXT_PUBLIC_BASE_URL=http://192.168.1.100:3000
# For production (Vercel), this is automatically detected
NEXT_PUBLIC_BASE_URL=

# Development settings
# Set this to your machine's IP address for mobile device access
# Find your IP with: ipconfig (Windows) or ifconfig (Mac/Linux)
# Example: NEXT_PUBLIC_DEV_IP=192.168.1.100
NEXT_PUBLIC_DEV_IP=

# Port for development server (default: 3000)
PORT=3000
